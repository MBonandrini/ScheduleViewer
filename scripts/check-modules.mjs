import fs from 'node:fs';import path from 'node:path';import {execFileSync} from 'node:child_process';
const root=process.cwd();const files=[];function walk(dir){for(const e of fs.readdirSync(dir,{withFileTypes:true})){const p=path.join(dir,e.name);if(e.isDirectory())walk(p);else if(/\.js$|\.mjs$/.test(e.name))files.push(p)}}walk(path.join(root,'src'));walk(path.join(root,'tests'));files.push(path.join(root,'sw.js'));
for(const f of files)execFileSync(process.execPath,['--check',f],{stdio:'pipe'});
const missing=[];for(const f of files.filter(x=>x.endsWith('.js'))){const text=fs.readFileSync(f,'utf8');for(const m of text.matchAll(/from\s+['"](\.\.?\/[^'"]+)['"]/g)){let target=path.resolve(path.dirname(f),m[1]);if(!path.extname(target))target+='.js';if(!fs.existsSync(target))missing.push(`${path.relative(root,f)} -> ${m[1]}`)}}if(missing.length){console.error('Missing relative imports:\n'+missing.join('\n'));process.exit(1)}console.log(`PASS: syntax checked ${files.length} JS files; all relative imports resolve.`);
