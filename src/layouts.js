const DEFAULT_COLUMNS=['task_code','task_name','wbs_id','target_drtn_hr_cnt','remain_drtn_hr_cnt','early_start_date','early_end_date','phys_complete_pct','total_float_hr_cnt'];
export function defaultLayout(){return {id:'default',name:'Classic Schedule Layout',columns:[...DEFAULT_COLUMNS],columnWidths:{},groupBy:'wbs_id',sort:[{field:'task_code',dir:1}],filters:[],rowHeight:'compact',timescale:'week',barStyle:'standard'}}
export function normalizeLayout(layout={}){const d=defaultLayout();return {...d,...layout,columns:Array.isArray(layout.columns)&&layout.columns.length?[...layout.columns]:[...d.columns],columnWidths:{...(layout.columnWidths||{})},sort:Array.isArray(layout.sort)&&layout.sort.length?layout.sort:[...d.sort],filters:Array.isArray(layout.filters)?layout.filters:[]}}
export function loadLayouts(key='uss-layouts'){try{const x=JSON.parse(localStorage.getItem(key)||'[]');return x.length?x.map(normalizeLayout):[defaultLayout()]}catch{return [defaultLayout()]}}
export function saveLayouts(layouts,key='uss-layouts'){localStorage.setItem(key,JSON.stringify(layouts.map(normalizeLayout)))}
export function upsertLayout(layouts,layout){const normalized=normalizeLayout(layout);const i=layouts.findIndex(x=>x.id===normalized.id);if(i>=0)layouts[i]=structuredClone(normalized);else layouts.push(structuredClone(normalized));return layouts}
export function removeLayout(layouts,id){return layouts.filter(x=>x.id!==id||id==='default')}
export function applyLayoutSort(rows,layout){const rules=layout?.sort||[];const groupBy=layout?.groupBy;return [...rows].sort((a,b)=>{if(groupBy){const g=String(a[groupBy]??'').localeCompare(String(b[groupBy]??''),undefined,{numeric:true});if(g)return g}for(const r of rules){const av=a[r.field]??'',bv=b[r.field]??'';const c=String(av).localeCompare(String(bv),undefined,{numeric:true});if(c)return c*(r.dir||1)}return 0})}
