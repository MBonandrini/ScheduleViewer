const DB='UnifiedScheduleStudio',STORE='projects';
export async function openDB(){return await new Promise((resolve,reject)=>{if(typeof indexedDB==='undefined')return reject(new Error('IndexedDB unavailable'));const r=indexedDB.open(DB,1);r.onupgradeneeded=()=>r.result.createObjectStore(STORE,{keyPath:'id'});r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)})}
export async function saveWorkspace(record){const db=await openDB();return await new Promise((res,rej)=>{const tx=db.transaction(STORE,'readwrite');tx.objectStore(STORE).put(record);tx.oncomplete=()=>res(record);tx.onerror=()=>rej(tx.error)})}
export async function listWorkspaces(){const db=await openDB();return await new Promise((res,rej)=>{const r=db.transaction(STORE).objectStore(STORE).getAll();r.onsuccess=()=>res(r.result||[]);r.onerror=()=>rej(r.error)})}
export async function getWorkspace(id){const db=await openDB();return await new Promise((res,rej)=>{const r=db.transaction(STORE).objectStore(STORE).get(id);r.onsuccess=()=>res(r.result||null);r.onerror=()=>rej(r.error)})}
export function makeProjectPackage({model,settings,baselines=[],bimModels=[],layouts=[],audit=[],scenarios=[]}){const safeBaselines=baselines.map(b=>({id:b.id,name:b.name,fileName:b.fileName,role:b.role,importedAt:b.importedAt,projId:b.projId,format:b.format||b.model?.sourceFormat||'',tables:b.model?[...b.model.tables.entries()].map(([n,t])=>[n,{name:t.name,fields:t.fields,rows:t.rows}]):b.tables||[]}));return {schema:'unified-schedule-project',version:1,createdAt:new Date().toISOString(),sourceFormat:model?.sourceFormat||'',header:model?.header||[],tables:model?[...model.tables.entries()].map(([n,t])=>[n,{name:t.name,fields:t.fields,rows:t.rows}]):[],settings,baselines:safeBaselines,bimModels,layouts,audit,scenarios}}
